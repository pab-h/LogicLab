import React from 'react'

const tabela = ({tabela}) => {
  return (
    <div className={`transition-all duration-500 ease-in-out ${tabela ? ' left-0 top-0 w-[80%]  bg-gray-500  font-bold rounded-tl-lg rounded-bl-lg' : 'fixed invisible top-[-100%]'}`}>
      <div className=' bottom-3 left-4 w-full h-full '>
      oi</div>
      </div>
  )
}

export default tabela